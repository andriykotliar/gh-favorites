import { AppRoutes } from "../../common";

const navigation = [
  {
    title: 'Home',
    path: AppRoutes.ROOT,
  },
  {
    title: 'Favorites',
    path: AppRoutes.FAVORITES,
  }
];

export { navigation };